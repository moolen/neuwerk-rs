# Firewall Repository Guidance

## Architectural Philosophy
- Strict separation of dataplane and control plane.
- Dataplane contains only packet processing and stateful NAT logic.
- Control plane handles DNS proxying, cluster replication, and future management APIs.
- No DNS parsing or control-plane logic inside the dataplane.

## NIC Model
- `data0` is DPDK-bound (VFIO) and owned by the dataplane.
- `mgmt0` is kernel-owned and used exclusively by the control plane.
- Dataplane must never process management traffic.
- Control plane must never process forwarded dataplane traffic.
- Cloud integrations must identify NICs via tags: `neuwerk.io/management` and `neuwerk.io/dataplane` on the cloud NIC resources.
  - Azure tags cannot contain `/`, so the Azure e2e stack uses `neuwerk.io.management` and `neuwerk.io.dataplane`; the control-plane accepts both spellings.

## Supported Traffic Flows
- DNS queries targeted at the firewall itself
- Firewall-originated upstream DNS traffic
- Firewall-originated cluster replication traffic
- Traffic under policy (DPDK data plane)

## Testing Model
- Packet-level unit tests in `tests/packet_unit.rs`.
- Integration tests in `tests/integration_nat.rs`.
- DPDK is isolated behind `dpdk_adapter.rs` and currently runs as a no-op dataplane.
- CI must run without NIC hardware or hugepages.
- SSO OIDC integration tests live in `tests/http_api/sso_oidc_cases.rs`; `make test.integration.sso` runs them, and `NEUWERK_SSO_REQUIRE_DEX=1` enforces fail-on-missing Dex instead of skip.

## Build Notes
- DPDK builds use a pinned, vendored DPDK source build (`third_party/dpdk/VERSION`) via `scripts/build-dpdk.sh` and `make build` (installs into `third_party/dpdk/install/<version>`).
- Makefile default `build` now prepares vendored DPDK and builds with `--all-features` (DPDK included) using `DPDK_DIR`/`PKG_CONFIG_PATH`; manual `--features dpdk` flags are no longer needed in Make targets.
- `DPDK_DIR` can point to a custom DPDK install prefix; `third_party/dpdk-sys` compiles a small wrapper C shim against those headers/libs.
- When building in a container, set `DPDK_DIR` to an absolute path (relative paths resolve from the dpdk-sys crate and will miss headers), and ensure `protobuf-compiler`, `libclang-14-dev`, and `llvm-14-dev` are installed so build scripts can run.
- DPDK build dependencies include `meson`, `ninja`, `python3`, `pkg-config`, `libnuma` headers, and Python `pyelftools`.
- Default vendored build disables `net/ionic` via `DPDK_DISABLE_DRIVERS` to avoid GCC 15 type conflicts; override as needed.
- Set `DPDK_FORCE_REBUILD=1` to rebuild an existing vendored DPDK install.
- Vendored DPDK patches live in `third_party/dpdk/patches` and are applied during `scripts/build-dpdk.sh`.
- Azure e2e images are Ubuntu 24.04 (glibc 2.39). Binaries built on newer glibc will fail to run; build the firewall binary inside a 24.04 (or older) environment or ship compatible runtime libs.
- Vendored DPDK 23.11.2 installs ABI `.so.24` (e.g., `librte_eal.so.24`). If you link against a different system DPDK (e.g., `.so.26`), you must install matching DPDK libs on the VM or the firewall binary will not start.
- Azure Germany West Central now offers Ubuntu 24.04 via `publisher=Canonical`, `offer=ubuntu-24_04-lts`, `sku=server` for the e2e stack.
- Azure MANA DPDK uses devargs `mac=` to select the NIC; kernel MANA drivers remain bound (no vfio binding). The dataplane can accept `--data-plane-interface mac:aa:bb:cc:dd:ee:ff` and map the DPDK port by MAC.
- Azure DPDK on NetVSC may miss ARP replies for the gateway in multiqueue mode; when `NEUWERK_CLOUD_PROVIDER=azure` the dataplane now falls back to `NEUWERK_AZURE_GATEWAY_MAC` (default `12:34:56:78:9a:bc`) to seed the gateway ARP entry.
- `scripts/setup-dex.sh` installs Dex for SSO tests; recent Dex releases may not publish prebuilt tarballs, so the script falls back to cloning the release tag and building `cmd/dex` (requires `go` and `git`).

## Design Constraints
- Favor correctness and deterministic behavior over micro-optimizations.
- Keep unsafe code isolated (only in `dpdk_adapter.rs` when added).
- Avoid global mutable state; use explicit state passing.
- NAT behavior must be deterministic and symmetric.

## Cloud Dataplane Assumptions
- DPDK dataplane is the target deployment mode for AWS/GCP/Azure.
- Dataplane IPv4 config (IP/prefix/gateway) is obtained via DHCP on the dataplane NIC; DHCP is mandatory.
- All policy source groups are treated as internal networks; the DHCP-derived prefix is the default internal group.
- In lifecycle-only integrations behind LB steering (AWS ASG / GCP MIG), do not keep scale-in protection enabled on active nodes; it can deadlock managed rollouts before termination hooks fire.
- VXLAN GWLB deployments with internal/external tunnels swap the tunnel/VNI on replies (internal -> external, external -> internal) when both tunnel configs are present.
- Dataplane answers ARP only for its dataplane IPv4 address (no public IP ARP).
- IPv4 fragments are dropped with metrics only (no logging).
- TTL is decremented on forwarded packets; ICMP Time Exceeded is generated when TTL expires.
- ICMP is fully supported in dataplane and policy-controlled (type/code allow/deny). Default policy allows common ICMP types.
- DNS allowlist enforcement is per source group (DNS policy decides which internal groups can access which hostnames).

## Future Extension Notes
- DPI may be added as a dedicated dataplane layer later.
- Any HTTPS proxying or DNS/HTTP parsing stays in the control plane.
- TLS verification should be done in the dataplane, allowing verification of the TLS client hello and server hello packets. We'll extend the policy engine to validate SNI, certificate hostnames or fingerprints.
- Dataplane must remain minimal and testable with pure Rust unit tests.
- Dataplane TLS validation is policy-driven per flow, uses TCP reassembly, and is fail-closed for TLS-constrained rules.
- TLS 1.3 certificates are uninspectable; policy must configure allow/deny behavior for those flows.
- Trust anchors for TLS validation come from the system store plus policy-embedded anchors; skip certificate time validity checks.
- AWS ASG integration now uses IMDSv2 role credentials + SigV4 AWS Query API calls (EC2 + Auto Scaling) for ASG member discovery, local identity lookup, instance protection, and termination lifecycle completion. In GWLB/NLB deployments this integration is lifecycle-only and does not modify VPC route tables. During drain it renews ASG terminating lifecycle hooks via `RecordLifecycleActionHeartbeat` near heartbeat expiry. IAM role permissions: `ec2:DescribeInstances`, `ec2:DescribeNetworkInterfaces`, `autoscaling:Describe*`, `autoscaling:SetInstanceProtection`, `autoscaling:RecordLifecycleActionHeartbeat`, and `autoscaling:CompleteLifecycleAction`.
- Azure VMSS integration and GCP MIG integration are now lifecycle-only in externally steered topologies (ILB/NLB/GWLB): they do not discover/manage route tables, and only handle instance discovery/readiness, termination drain signaling, and (where supported) instance protection.
- GCP MIG integration normalizes label keys like `neuwerk-io-cluster`/`neuwerk-io-role` into `neuwerk.io/cluster`/`neuwerk.io/role` for discovery-tag matching compatibility.
- `IntegrationCapabilities.lifecycle_hook` is currently informational only; integration runtime behavior is gated by `instance_protection` and `termination_notice`.

## Self-Improving Prompt
- If you learn something important about this repository, its constraints, or workflows, add it to this `AGENTS.md` so future work benefits from it.
### UI Notes
- The React UI lives under `ui/` and is served by the control-plane HTTPS server from `ui/dist`.
- Control-plane HTTP API routes are rooted at `/api/v1` (UI calls `/api/v1/*`).
- Policies are now managed via a form-driven builder on `ui/pages/PoliciesPage.tsx` with strict typed models (`ui/utils/policyModel.ts`) and backend-parity client validation (`ui/utils/policyValidation.ts`).
- Policy page orchestration is now split: `ui/pages/PoliciesPage.tsx` is the shell, state/load/save logic lives in `ui/pages/policies/usePolicyBuilder.ts`, shared helpers live in `ui/pages/policies/helpers.ts`, and reusable modules live under `ui/pages/policies/components/`.
- Policy builder composition is now sectioned: `PolicyBuilderForm.tsx` is a thin shell, `PolicyBasicsSection.tsx` owns mode/default controls, `SourceGroupsSection.tsx` is the source-group routing shell, `SourceGroupCard.tsx` is a thin orchestrator for `SourceGroupHeaderSection.tsx` + `SourceGroupSourcesSection.tsx` + `SourceGroupRulesSection.tsx`, `KubernetesSourcesEditor.tsx` owns Kubernetes source list/card editing, `RuleEditor.tsx` is a thin orchestrator for `RuleHeaderSection.tsx` + `RuleMatchCriteriaSection.tsx` + `RuleTlsSection.tsx`, `TlsMetadataSection.tsx` owns TLS metadata constraints, and `TlsInterceptHttpSection.tsx` orchestrates `TlsInterceptRequestSection.tsx` + `TlsInterceptResponseSection.tsx`. Keep future changes in those section modules instead of regrowing `PoliciesPage.tsx`/`PolicyBuilderForm.tsx`.
- Policy editor card is now further split: `PolicyEditorCard.tsx` is a shell over `PolicyEditorHeader.tsx` (builder/yaml tab + subtitle controls) and `PolicyEditorMessages.tsx` (validation/error panels); keep future card chrome/message updates in those focused modules.
- Policy snapshots rendering is now split: `PolicySnapshotsPanel.tsx` is a shell over `PolicySnapshotRow.tsx`, and snapshot id/timestamp formatting helpers live in `policySnapshotHelpers.ts`.
- TLS intercept request editing is further split: `TlsInterceptRequestSection.tsx` now composes `TlsInterceptRequestQuerySection.tsx` and `TlsInterceptRequestHeadersSection.tsx`; keep query/header matcher changes in those dedicated modules.
- TLS intercept request shell is further decomposed: `TlsInterceptRequestSection.tsx` now delegates methods editing to `TlsInterceptRequestMethodsField.tsx` and path editing to `TlsInterceptRequestPathSection.tsx`; keep method/path UI changes in those focused components.
- TLS intercept HTTP section is now further decomposed: `TlsInterceptHttpSection.tsx` composes `TlsInterceptConstraintControls.tsx` for request/response toggle controls, while guarded request/response enable/disable draft mutations are centralized in `tlsInterceptHttpDraft.ts`.
- TLS intercept request draft mutation/defaulting logic is centralized in `ui/pages/policies/components/tlsInterceptRequestDraft.ts`; keep request/query/header draft access and default-initialization behavior in this helper instead of duplicating path-based update blocks.
- Policy page helpers are now domain-split: `ui/pages/policies/helpers.ts` is a compatibility shell, conversion helpers live in `ui/pages/policies/valueCodec.ts`, and draft/id/move/issue helpers live in `ui/pages/policies/policyDraftHelpers.ts`.
- Policy validation decomposition is now domain-split: `ui/utils/policyValidation.ts` is a thin orchestration shell, source-group checks live in `ui/utils/policyValidation/sourceGroupValidation.ts`, rule core checks live in `ui/utils/policyValidation/ruleMatchValidation.ts`, TLS mode/matcher checks live in `ui/utils/policyValidation/tlsMatchValidation.ts`, and shared TLS HTTP matcher helpers remain in `ui/utils/policyValidation/tlsHttpValidation.ts`.
- Rule-match validation is now further sectioned under `ui/utils/policyValidation/ruleMatchValidation/`: basic rule field checks in `basics.ts`, proto/ports/dns/icmp checks in `matchCore.ts`, and `ruleMatchValidation.ts` as a compatibility shell.
- TLS-match validation is now further sectioned under `ui/utils/policyValidation/tlsMatchValidation/`: metadata matcher checks in `metadata.ts`, intercept/metadata mode semantics in `mode.ts`, shared issue shape in `types.ts`, and `tlsMatchValidation.ts` as the orchestration shell.
- Policy model decomposition is now split into submodules: factories/templates in `ui/utils/policyModel/factories.ts`, id allocation in `ui/utils/policyModel/ids.ts`, normalization in `ui/utils/policyModel/normalize/` (`shared.ts`, `tls.ts`, `sources.ts`, `rules.ts` with a thin `normalize.ts` shell), and API sanitization in `ui/utils/policyModel/sanitize/` (`shared.ts`, `tls.ts`, `sources.ts`, `rules.ts`, `request.ts` with a thin `sanitize.ts` shell); `ui/utils/policyModel.ts` is a thin compatibility re-export shell.
- Policy-model rule templates now live in `ui/utils/policyModel/ruleTemplates.ts`; keep `factories.ts` focused on empty defaults + template entrypoints.
- Integrations UI is now split like policies: `ui/pages/IntegrationsPage.tsx` is a shell, orchestration/state live in `ui/pages/integrations/useIntegrationsPage.ts`, and rendering is split across `ui/pages/integrations/components/{IntegrationsHeader,IntegrationsListPanel,IntegrationEditorPanel}.tsx`.
- Integration editor rendering is further sectioned: `IntegrationEditorPanel.tsx` is now a shell over `IntegrationEditorHeader.tsx`, `IntegrationBasicsSection.tsx`, `IntegrationCredentialsSection.tsx`, and `IntegrationEditorActions.tsx`; keep future editor field/control changes in those section modules.
- Integrations hook decomposition now separates concerns: remote API orchestration lives in `ui/pages/integrations/remote.ts`, form validation/normalization lives in `ui/pages/integrations/formValidation.ts`, and `useIntegrationsPage.ts` remains the state orchestration shell.
- Integrations lifecycle orchestration is now operation-split under `ui/pages/integrations/lifecycle/` (`createNew.ts`, `select.ts`, `load.ts`, `save.ts`, `delete.ts`); keep `useIntegrationsPageLifecycle.ts` as a thin composition shell.
- Wiretap UI is now split: `ui/pages/WiretapPage.tsx` is a shell, orchestration/state live in `ui/pages/wiretap/useWiretapPage.ts`, and render modules live in `ui/pages/wiretap/components/`; wiretap pause/resume no longer recreates the SSE subscription on each toggle.
- Wiretap runtime orchestration is now further split: SSE connect/reconnect lifecycle lives in `ui/pages/wiretap/useWiretapConnection.ts`, and event upsert/buffer/default-filter state helpers live in `ui/pages/wiretap/state.ts`; keep `useWiretapPage.ts` as the composition hook.
- Wiretap aggregated view is further split: `WiretapAggregatedTable.tsx` is a shell, `WiretapAggregatedTableRow.tsx` owns row rendering, and aggregate table headers/flow-label helpers live in `aggregatedTableHelpers.ts`.
- Wiretap live view is further split: `WiretapLiveTable.tsx` is a shell, `WiretapLiveTableRow.tsx` owns live row rendering, and live table headers/flow-label helpers live in `liveTableHelpers.ts`.
- `ui/components/WiretapFilters.tsx` is now a shell over `ui/components/wiretap/WiretapFilterFields.tsx` (field inputs) and `ui/components/wiretap/WiretapStreamControls.tsx` (pause/clear/status/event-count controls); keep future filter/control UI changes in those focused modules.
- Service accounts UI is now split: `ui/pages/ServiceAccountsPage.tsx` is a shell, orchestration/state live in `ui/pages/service-accounts/useServiceAccountsPage.ts`, and token UI components live in `ui/pages/service-accounts/components/`.
- Service-account orchestration is now further split: account API operations are in `ui/pages/service-accounts/remote.ts`, token-panel orchestration lives in `ui/pages/service-accounts/useServiceAccountTokenPanel.ts`, and shared error mapping lives in `ui/pages/service-accounts/helpers.ts`; keep `useServiceAccountsPage.ts` as a thin composition shell.
- Service-account token panel rendering is now split: `ServiceAccountTokensPanel.tsx` is a shell, `ServiceAccountTokensTable.tsx` owns table structure, `ServiceAccountTokenTableRow.tsx` owns row actions/status rendering, and timestamp/revoke helpers live in `tokenTableHelpers.ts`.
- Service-account token modal is now split: `CreateTokenModal.tsx` is a shell, `CreateTokenModalFields.tsx` owns name/TTL/eternal inputs + TTL preset chips, `CreateTokenModalActions.tsx` owns action buttons, and request normalization/preset constants live in `createTokenForm.ts`.
- `ui/components/service-accounts/ServiceAccountTable.tsx` is now a shell over `ServiceAccountTableRow.tsx`, `ServiceAccountStatusBadge.tsx`, and `ServiceAccountTableEmptyState.tsx`; shared formatting/status helpers live in `ui/components/service-accounts/helpers.ts`.
- `ui/components/service-accounts/CreateServiceAccountModal.tsx` is now a shell over `CreateServiceAccountModalFields.tsx` and `CreateServiceAccountModalActions.tsx`; request normalization/validation lives in `ui/components/service-accounts/createForm.ts`.
- Login UI is now split: `ui/components/auth/LoginPage.tsx` is a shell, submit/state logic lives in `ui/components/auth/useTokenLogin.ts`, token form rendering is in `ui/components/auth/LoginTokenForm.tsx`, and input/error helpers live in `ui/components/auth/loginHelpers.ts`.
- Login page SSO is now backend-driven: provider buttons render from `GET /api/v1/auth/sso/providers` via `ui/components/auth/useSsoProviders.ts`, and clicking a provider redirects via `buildSsoStartPath(...)` to `/api/v1/auth/sso/:id/start`.
- Local UI preview auth bypass: on localhost (`localhost`, `127.0.0.1`, `::1`), clicking Sign in with an empty token writes a local preview auth user to `localStorage` so `npm run preview` can reach the dashboard without backend auth; `AuthProvider` reads this state and `logout` clears it.
- App routing/navigation metadata is centralized in `ui/navigation.ts` (canonical page IDs + nav items + path helpers), and page rendering is mapped in `ui/app/renderPage.tsx`; `App.tsx` and `Sidebar.tsx` should consume those shared definitions.
- Dashboard UI is now split: `ui/pages/Dashboard.tsx` is a shell, polling/state logic lives in `ui/pages/dashboard/useDashboardStats.ts`, and render/layout logic lives in `ui/pages/dashboard/components/`.
- Dashboard stats rendering is now further sectioned: `DashboardStatsView.tsx` is a thin shell over `DashboardHeader.tsx`, `DashboardOverviewCards.tsx`, `DashboardDataplaneSection.tsx`, `DashboardControlPlaneSection.tsx`, `DashboardBytesSection.tsx`, `DashboardSystemSection.tsx`, and `DashboardRaftCatchupSection.tsx`, with shared card frame styles in `DashboardSectionCard.tsx`.
- Audit UI is now split: `ui/pages/AuditPage.tsx` is a shell, query/filter state and loading logic live in `ui/pages/audit/useAuditPage.ts`, and table/filter/error rendering lives in `ui/pages/audit/components/`.
- DNS cache UI is now split: `ui/pages/DNSCachePage.tsx` is a shell, data load/filter state lives in `ui/pages/dns-cache/useDNSCachePage.ts`, and rendering/controls are split into `ui/pages/dns-cache/components/`.
- Settings UI is now split: `ui/pages/SettingsPage.tsx` is a shell, TLS intercept CA + SSO provider orchestration lives in `ui/pages/settings/useSettingsPage.ts`, and rendering is split into `ui/pages/settings/components/` (including `SsoProvidersForm.tsx`).
- Settings includes a one-click DPI/TLS intercept CA generator in `ui/pages/settings/components/TlsInterceptCaForm.tsx` wired through `useSettingsPage.ts` to `POST /api/v1/settings/tls-intercept-ca/generate`; generation persists material through the same backend storage path as manual Save.
- UI API client is now domain-split: `ui/services/api.ts` is a compatibility barrel, shared fetch/error transport lives in `ui/services/apiClient/transport.ts`, and domain clients live in `ui/services/apiClient/{auth,policies,integrations,dns,audit,wiretap,serviceAccounts,settings,sso}.ts`.
- UI shared types are now domain-split: `ui/types.ts` is a compatibility barrel, and feature type domains live under `ui/types/` (`policy`, `integrations`, `stats`, `dns`, `wiretap`, `audit`, `auth`, `serviceAccounts`, `settings`).
- UI API error parsing in `ui/services/api.ts` now prioritizes backend `{ error: "..." }`, falls back to `{ message: "..." }`, then status text.
- UI auth is cookie-backed only: legacy in-memory token cache helpers were removed from `ui/services/api.ts`.
- Wiretap SSE in UI uses credentialed EventSource (`withCredentials: true`) and must not append auth tokens to query strings.
- UI regression tests now run via `npm test` (Vitest) and currently include `ui/services/api.test.ts` assertions for cookie-only auth transport and wiretap SSE credential semantics.
- UI regression tests now also include helper coverage for DNS cache filtering/timestamp formatting (`ui/pages/dns-cache/helpers.test.ts`), TLS intercept CA input validation (`ui/pages/settings/helpers.test.ts`), service-account helper formatting/status helpers (`ui/components/service-accounts/helpers.test.ts`), and policy validation behavior (`ui/utils/policyValidation.test.ts`).
- UI regression tests now also include service-account create-request validation (`ui/components/service-accounts/createForm.test.ts`) and integration form validation (`ui/pages/integrations/formValidation.test.ts`).
- UI regression tests now also include service-account page helper coverage (`ui/pages/service-accounts/helpers.test.ts`) and login helper coverage (`ui/components/auth/loginHelpers.test.ts`).
- UI regression tests now also include token-table helper coverage (`ui/pages/service-accounts/components/tokenTableHelpers.test.ts`).
- UI regression tests now also include policy helper coverage (`ui/pages/policies/helpers.test.ts`).
- UI regression tests now also include wiretap aggregated table helper coverage (`ui/pages/wiretap/components/aggregatedTableHelpers.test.ts`).
- UI regression tests now also include wiretap live-table helper coverage (`ui/pages/wiretap/components/liveTableHelpers.test.ts`).
- UI regression tests now also include TLS intercept request draft helper coverage (`ui/pages/policies/components/tlsInterceptRequestDraft.test.ts`).
- UI regression tests now also include token-create form helper coverage (`ui/pages/service-accounts/components/createTokenForm.test.ts`).
- TLS intercept request draft tests now also cover path defaulting via `ensureTlsRequestPath`; keep future request path defaulting changes covered there.
- UI regression tests now also include policy snapshot helper coverage (`ui/pages/policies/components/policySnapshotHelpers.test.ts`).
- UI regression tests now also include rule-match draft mutation helper coverage (`ui/pages/policies/components/ruleMatchDraft.test.ts`) and kubernetes-source draft mutation helper coverage (`ui/pages/policies/components/kubernetesSourceDraft.test.ts`).
- UI regression tests now also include TLS rule draft mutation/mode-toggle coverage (`ui/pages/policies/components/ruleTlsDraft.test.ts`).
- UI regression tests now also include source-group rules template helper coverage (`ui/pages/policies/components/sourceGroupRulesDraft.test.ts`).
- UI regression tests now also include rule-match protocol/dns helper coverage (`ui/pages/policies/components/ruleMatchProtocolDraft.test.ts`).
- UI regression tests now also include policy-builder draft mutation coverage (`ui/pages/policies/policyBuilderDraftMutations.test.ts`).
- UI regression tests now also include policy-builder remote orchestration coverage (`ui/pages/policies/policyBuilderRemote.test.ts`).
- UI regression tests now also include policy-builder derived-state helper coverage (`ui/pages/policies/usePolicyBuilderDerived.test.ts`).
- UI regression tests now also include dashboard helper coverage (`ui/pages/dashboard/helpers.test.ts`).
- UI regression tests now also include TLS HTTP matcher validation helper coverage (`ui/utils/policyValidation/tlsHttpValidation.test.ts`).
- UI regression tests now also include sidebar helper coverage (`ui/components/sidebar/helpers.test.ts`).
- UI regression tests now also include TLS policy-model normalization helper coverage (`ui/utils/policyModel/normalize/tls.test.ts`).
- UI regression tests now also include policy-builder lifecycle helper coverage (`ui/pages/policies/policyBuilderLifecycle.test.ts`).
- UI regression tests now also include rule-header draft helper coverage (`ui/pages/policies/components/ruleHeaderDraft.test.ts`).
- UI regression tests now also include source-group header draft helper coverage (`ui/pages/policies/components/sourceGroupHeaderDraft.test.ts`).
- UI regression tests now also include policy-model API sanitize coverage (`ui/utils/policyModel/sanitize.test.ts`).
- UI regression tests now also include TLS match validation coverage (`ui/utils/policyValidation/tlsMatchValidation.test.ts`), rule-match validation coverage (`ui/utils/policyValidation/ruleMatchValidation.test.ts`), policy-model factory/template coverage (`ui/utils/policyModel/factories.test.ts`), integrations save-lifecycle helper coverage (`ui/pages/integrations/lifecycle/save.test.ts`), and wiretap state helper coverage (`ui/pages/wiretap/state.test.ts`).
- UI regression tests now also include TLS-intercept HTTP draft toggle coverage (`ui/pages/policies/components/tlsInterceptHttpDraft.test.ts`) and string-list-map mutation coverage (`ui/pages/policies/components/stringListMapDraft.test.ts`).
- Key/value editor state mutation logic is isolated in `ui/components/key-value/state.ts` with regression coverage in `ui/components/key-value/state.test.ts`; keep entry add/rename/remove semantics in this helper module.
- Key/value editor rendering is now decomposed: `ui/components/KeyValueEditor.tsx` is a shell, row rendering is in `ui/components/key-value/KeyValueEntryRow.tsx`, reusable input+error rendering is in `ui/components/key-value/KeyValueEntryInput.tsx`, header/add button is in `ui/components/key-value/KeyValueEditorHeader.tsx`, and empty-state rendering is in `ui/components/key-value/KeyValueEditorEmptyState.tsx`.
- Key/value field error-path helpers live in `ui/components/key-value/fieldErrors.ts` with regression coverage in `ui/components/key-value/fieldErrors.test.ts`; keep key/value error path generation centralized there.
- Rule match criteria UI is now decomposed: `ui/pages/policies/components/RuleMatchCriteriaSection.tsx` is a shell over `RuleMatchProtocolDnsSection.tsx`, `RuleMatchDestinationSection.tsx`, `RuleMatchPortsSection.tsx`, and `RuleMatchIcmpSection.tsx`; guarded match mutation logic is centralized in `ui/pages/policies/components/ruleMatchDraft.ts`.
- Kubernetes source editor UI is now decomposed: `ui/pages/policies/components/KubernetesSourcesEditor.tsx` is a shell over `KubernetesSourceCard.tsx`; card internals are split into `KubernetesSourceIntegrationRow.tsx`, `KubernetesSourceSelectorTypeRow.tsx`, `KubernetesPodSelectorFields.tsx`, and `KubernetesNodeSelectorFields.tsx`; guarded source mutation logic is centralized in `ui/pages/policies/components/kubernetesSourceDraft.ts`.
- TLS rule editor UI is now decomposed: `ui/pages/policies/components/RuleTlsSection.tsx` is a shell over `RuleTlsHeader.tsx`, `RuleTlsModeControls.tsx`, and mode-specific sections; TLS metadata rendering is decomposed into `TlsMetadataNameMatchersSection.tsx`, `TlsMetadataServerDnField.tsx`, `TlsMetadataFingerprintField.tsx`, and `TlsMetadataTrustAnchorsSection.tsx`; guarded TLS mutation/mode helpers are centralized in `ui/pages/policies/components/ruleTlsDraft.ts`.
- Source-group rules UI is now decomposed: `ui/pages/policies/components/SourceGroupRulesSection.tsx` is a shell over `SourceGroupRulesToolbar.tsx`, `SourceGroupRulesList.tsx`, and `SourceGroupRulesEmptyState.tsx`; template selection/update helpers are centralized in `ui/pages/policies/components/sourceGroupRulesDraft.ts`.
- Rule match protocol+DNS UI is now decomposed: `ui/pages/policies/components/RuleMatchProtocolDnsSection.tsx` is a shell over `RuleMatchProtocolField.tsx` and `RuleMatchDnsHostnameField.tsx`; protocol-selection and DNS-normalization helpers are centralized in `ui/pages/policies/components/ruleMatchProtocolDraft.ts`.
- Policies page rendering is now further sectioned: `ui/pages/PoliciesPage.tsx` is a shell over `PoliciesPageHeader.tsx`, `PolicyEditorCard.tsx`, and `PolicyEditorActions.tsx`; page subtitle/save-disabled helpers live in `ui/pages/policies/components/policiesPageHelpers.ts` with regression tests in `policiesPageHelpers.test.ts`.
- TLS HTTP matcher validation is now sectioned under `ui/utils/policyValidation/tlsHttpValidation/`: shared `types.ts` + `regex.ts`, TLS-name matcher logic in `tlsName.ts`, request matcher validation in `request.ts`, and response-header matcher validation in `response.ts`; keep `tlsHttpValidation.ts` as a thin compatibility shell.
- Sidebar UI is now split: `ui/components/Sidebar.tsx` is a shell over `ui/components/sidebar/SidebarHeader.tsx`, `SidebarNav.tsx`, `SidebarNavItem.tsx`, and `SidebarFooter.tsx`; icon/logo constants live in `ui/components/sidebar/constants.tsx`, and nav filtering/style/hover helpers live in `ui/components/sidebar/helpers.ts`.
- TLS normalization is now sectioned under `ui/utils/policyModel/normalize/tls/`: name matcher logic in `name.ts`, HTTP matcher normalization in `http.ts`, and top-level TLS match normalization in `match.ts`; keep `normalize/tls.ts` as a thin compatibility shell.
- TLS sanitize logic is now sectioned under `ui/utils/policyModel/sanitize/tls/`: name matcher sanitization in `name.ts`, HTTP matcher sanitization in `http.ts`, and top-level TLS match sanitization in `match.ts`; keep `sanitize/tls.ts` as a thin compatibility shell.
- Policy builder group/rule lifecycle mutations are now centralized in `ui/pages/policies/policyBuilderDraftMutations.ts`; keep `usePolicyBuilder.ts` focused on orchestration/state wiring and add future mutation behavior changes to that helper module with tests.
- Policy builder action wiring is now centralized in `ui/pages/policies/policyBuilderDraftActions.ts`; keep add/duplicate/move/delete group/rule action closures there and keep `usePolicyBuilder.ts` focused on lifecycle orchestration.
- Policy builder remote API orchestration is now centralized in `ui/pages/policies/policyBuilderRemote.ts`; keep policy list/load/save/delete transport behavior there and keep `usePolicyBuilder.ts` focused on state/lifecycle wiring.
- Policy builder derived state is now centralized in `ui/pages/policies/usePolicyBuilderDerived.ts`; keep integration-name derivation and validation issue derivation in that helper hook/module.
- Policy builder lifecycle orchestration is now sectioned: `ui/pages/policies/policyBuilderLifecycle.ts` is a compatibility shell, flow control lives in `policyBuilderLifecycleFlow.ts`, helper decisions/errors live in `policyBuilderLifecycleHelpers.ts`, and lifecycle dependency/handler contracts live in `policyBuilderTypes.ts`; keep `usePolicyBuilder.ts` as a thin composition shell.
- Rule header UI is now decomposed: `RuleHeaderSection.tsx` is a shell over `RuleHeaderFields.tsx` and `RuleHeaderActions.tsx`; guarded rule-header mutation helpers live in `ruleHeaderDraft.ts`.
- Source-group header UI is now decomposed: `SourceGroupHeaderSection.tsx` is a shell over `SourceGroupHeaderFields.tsx` and `SourceGroupHeaderActions.tsx`; guarded source-group header mutation helpers live in `sourceGroupHeaderDraft.ts`.
- `StringListMapEditor.tsx` is now a shell over `StringListMapEntryRow.tsx` and `StringListMapEmptyState.tsx`; key/value-list row mutation semantics are centralized in `stringListMapDraft.ts`.
- Policy-model API sanitize logic is now decomposed under `ui/utils/policyModel/sanitize/` (`request.ts`, `sources.ts`, `rules.ts`, `tls.ts`, `shared.ts`) with `sanitize.ts` as a compatibility shell; keep sanitize behavior changes in those domain modules and extend `sanitize.test.ts`.
- UI dependency security is pinned with an override: `ui/package.json` forces `rollup@^4.59.0` to remediate `GHSA-mw96-cpmx-2vgc`; keep `npm audit` clean when bumping Vite/Vitest.

## Control-Plane Storage Notes
- Local (non-cluster) policies live in `/var/lib/neuwerk/local-policy-store`.
- Local (non-cluster) service accounts and token metadata live in `/var/lib/neuwerk/service-accounts`.
- Local integration records now seal `service_account_token` into `service_account_token_envelope`; plaintext tokens are not persisted to disk.
- Local integration and service-account store files are written with explicit `0600` permissions (including atomic-temp files).
- Local (non-cluster) API auth keyset lives in `/var/lib/neuwerk/http-tls/api-auth.json`.
- `run_http_api` local-mode wiring now derives service-account/integration store paths from the local policy store root (sibling directories), so tests should use a temp local policy store root to keep all mutable HTTP API state writable and isolated.
- Cluster mode stores policies, service accounts, API auth keyset, and CA material in the Raft-backed RocksDB store under `/var/lib/neuwerk/cluster/raft`.
- HTTP TLS CA cert and private key should be persisted in local mode (proposed `http-tls/ca.key` alongside `ca.crt`).
- TLS intercept CA local files are `http-tls/intercept-ca.crt` and `http-tls/intercept-ca.key`; cluster migration seeds them into Raft keys `settings/tls_intercept/ca_cert_pem` and `settings/tls_intercept/ca_key_envelope` when present.
- Policy rebuilds clear the DNS allowlist so deny updates take effect immediately.
- Policy rebuilds bump a generation counter so the dataplane re-evaluates existing flows on their next packet (soft-cut enforcement).
- Control-plane tracks the active policy ID to avoid redundant rebuilds during cluster replication.
- The built-in `internal` DNS allowlist source group is intentionally lowest-priority; explicit source groups (including TLS intercept rules) must evaluate first to prevent DNS allowlist bypass of higher-layer policy.

## Runtime CLI
- The binary requires `--management-interface`, `--data-plane-interface`, at least one `--dns-target-ip` (or `--dns-target-ips` CSV), and at least one `--dns-upstream` (or `--dns-upstreams` CSV) to start. CSV and repeated forms are mutually exclusive per setting.
- The software dataplane uses `--data-plane-mode tun|tap` (default `tun`) and attaches to a Linux TUN/TAP device. `dpdk` is accepted for DPDK mode; real DPDK IO requires the `dpdk` cargo feature and a system DPDK install.
- NAT/flow idle eviction is controlled by `--idle-timeout-secs` (default 300, must be >= 1).
- DNS allowlist GC is controlled by `--dns-allowlist-idle-secs` (default `idle-timeout + 120`, must be >= 1).
- DNS allowlist GC interval is controlled by `--dns-allowlist-gc-interval-secs` (default 30, must be >= 1).
- Default policy is controlled by `--default-policy allow|deny` (default `deny`).
- DHCP tuning flags: `--dhcp-timeout-secs` (default 5), `--dhcp-retry-max` (default 5), `--dhcp-lease-min-secs` (default 60), all must be >= 1.
- Internal network override: `--internal-cidr <cidr>` sets the default internal group and disables DHCP-based internal CIDR updates when set.
- `--snat none|auto|<ipv4>` controls SNAT (software only for `<ipv4>`); DPDK mode relies on DHCP.
- In software `tun` mode with policy routing (`iif <mgmt> lookup 100` -> `fwdp0`), seeing an unsnat'd packet on `fwdp0 Out` is expected (kernel -> TUN pre-userspace rewrite). The rewritten packet re-enters as `fwdp0 In`.
- In that same `tun` setup, `net.ipv4.conf.*.accept_local=0` can drop rewritten packets when SNAT uses a local interface IP; enabling `accept_local` allows egress, but return traffic to the SNAT IP can still be consumed by kernel local delivery (`ip rule` pref `0` local table) and trigger local TCP RSTs unless reply steering is handled before local lookup. Prefer DPDK/data NIC topology for production-like return-path behavior.
- Overlay flags: `--encap none|vxlan|geneve`, `--encap-vni <id>`, `--encap-udp-port <port>`, `--encap-vni-internal`, `--encap-vni-external`, `--encap-udp-port-internal`, `--encap-udp-port-external`, `--encap-mtu <bytes>`.
- Azure GWLB VXLAN defaults: UDP `10800` (internal tunnel) and UDP `10801` (external tunnel).
- `--management-interface` and `--data-plane-interface` must not be the same interface.
- In DPDK mode, the process exits if DHCP fails to obtain a lease.
- Runtime CLI parsing/help text and config types now live under `src/runtime/cli/` (`mod.rs`, `args.rs`, `parse_helpers.rs`, `types.rs`, `usage.rs`); keep external callsites using `runtime::cli::*` re-exports. Runtime startup orchestration helpers live under `src/runtime/bootstrap/` (`startup.rs` for seed-selection/config logging, `task_wait.rs` for terminal runtime task select/await). DPDK dataplane run-loop orchestration lives in `src/runtime/dpdk/run.rs`; `src/main.rs` unit tests live in `src/main_tests.rs`.
- `src/e2e/services.rs` is now a thin module/re-export shell; keep new e2e service logic in domain modules under `src/e2e/services/` (`http_api.rs`, `dns.rs`, `server_runtime.rs`, `tls.rs`, `udp.rs`, `upstream.rs`, `models.rs`) and keep `tests.rs` for service helper unit tests.
- `src/e2e/tests/traffic_cases.rs` now delegates cluster-policy update scenarios to `src/e2e/tests/traffic_cases/cluster_policy.rs` via thin wrappers to keep case-catalog wiring stable.
- `src/controlplane/metrics.rs` now keeps registry construction plus snapshot/render logic; high-frequency observe/inc/set methods live in `src/controlplane/metrics/methods.rs`.
- `src/controlplane/policy_config.rs` now keeps core policy/group/rule config wiring; TLS/HTTP intercept policy schema+compile logic lives in `src/controlplane/policy_config/tls_http.rs`, parsing helpers remain in `src/controlplane/policy_config/parse.rs`, and tests live in `src/controlplane/policy_config/tests.rs`.
- Policy management is via HTTPS API on `--http-bind` (default management IP `:8443`) using `POST /api/v1/policies` and `GET /api/v1/policies`; `/ready` is available for readiness checks.
- `/ready` now returns not-ready while integration drain mode is active (so LBs can stop new scheduling), but dataplane flow admission itself is not drain-blocked and can still accept new flows until instance termination.
- `POST`/`PUT`/`DELETE /api/v1/policies` block until the dataplane observes the new policy generation when the dataplane is running (2s timeout; returns `503` on activation timeout). When no dataplane is running (e.g., control-plane-only cluster tests), they return immediately.
- Service-lane runtime ensures `svc0` (TAP) exists with `169.254.255.1/30`. In DPDK mode, TLS intercept steering uses dataplane packet demux: intercept-eligible client flows are rewritten to `169.254.255.1:15443` on `svc0` and mapped by `(client_ip,client_port)` so service-lane egress packets can be rewritten back to the original upstream tuple before DPDK TX.
- TLS intercept HTTP/2 handling in `intercept_runtime.rs` uses bounded stream concurrency and timeout-bound request/upstream body reads to keep browser fan-out responsive while remaining fail-closed under stalls.
- TLS intercept HTTP/2 request-body handling must release `RecvStream` flow-control capacity per chunk; missing release can stall large uploads and eventually fail-closed.
- TLS intercept HTTP/2 upstream-response handling must release `RecvStream` flow-control capacity for each chunk; missing `release_capacity` causes long responses (e.g., `github.com`) to stall and fail-closed with `tls intercept: h2 upstream body read timed out`.
- TLS intercept HTTP/2 now uses bounded stream concurrency (env `NEUWERK_TLS_H2_MAX_CONCURRENT_STREAMS`, default `64`) instead of single-stream serialization; browsers like GitHub can issue hundreds of concurrent H2 asset requests and otherwise accumulate multi-second `blocked` queueing.
- TLS intercept HTTP/2 upstream requests must forward browser headers (e.g., `Accept`, `X-Requested-With`, `Sec-Fetch-*`) while stripping hop-by-hop headers; rebuilding H2 requests with only method/path/host can trigger GitHub async endpoint failures (`400/406`) and partial page render.
- TLS intercept H2 request-body idle timeout is configurable via `NEUWERK_TLS_H2_BODY_TIMEOUT_SECS` (default 10s in `trafficd.rs`); use this only for controlled perf troubleshooting because larger values trade fail-closed speed for tolerance under upstream/client saturation.
- In DPDK mode, the adapter now drains egress packets from `svc0` and emits them on DPDK TX after standard L2 rewrite/ARP resolution; service-lane return-path packets can trigger ARP requests when neighbor MAC is not cached.
- DPDK intercept steering foundation: when `svc0` TAP is attachable, the dataplane can emit `Action::ToHost` for intercept-eligible flows and the DPDK adapter writes those frames to `svc0` (env override: `NEUWERK_DPDK_SERVICE_LANE_IFACE`, default `svc0`).
- TLS intercept runtime now mints per-host leaf certificates from SNI using the configured intercept CA and caches minted certs in-memory (15m TTL, 1024-entry bound).
- TLS intercept CA `PUT`/`DELETE` now bumps a CA-generation signal; `trafficd` restarts the live intercept runtime when that generation changes so new leaf certs are minted from the updated CA without process restart.
- Startup now waits for DNS/service-plane runtime initialization before marking readiness (`dns`/`service_plane`), with a 2s startup timeout; DNS bind/config failures fail process startup early.
- HTTP API wiretap stream auth accepts bearer/cookie auth only; query-string token fallback is rejected.
- HTTP API response hardening includes CSP, frame/referrer/content-type protections, permissions policy, and HSTS; auth routes also return `Cache-Control: no-store`.
- `POST /api/v1/auth/token-login` has an in-process failed-login limiter (20 failures / 60s window => 120s block) with bounded scoped buckets (`client hint + token fingerprint`, max 4096 buckets, LRU eviction) and should return `429` while the matching bucket is blocked.
- Login limiter client hint prefers peer socket identity (`ConnectInfo<SocketAddr>`) and only falls back to forwarding headers when peer metadata is unavailable; avoid trusting raw forwarding headers on direct connections.
- `POST /api/v1/auth/token-login` enforces a token-size guardrail (`AUTH_LOGIN_MAX_TOKEN_LEN=8192`) and returns `400` for oversized token fields before validation.
- Azure provider construction is now fallible: `AzureProvider::new(...) -> Result<_, CloudError>`, and runtime integration bootstrap propagates init failures explicitly instead of panicking.
- HTTP API decomposition is in progress under `src/controlplane/http_api/`; extracted modules now include `proxy.rs`, `auth.rs`, `security.rs`, `metrics.rs`, `extractors.rs`, `wiretap.rs`, `policy.rs`, `integrations.rs`, `service_accounts_api.rs`, and `audit.rs`.
- HTTP API route decomposition now also includes `src/controlplane/http_api/auth_routes.rs` (token-login/whoami/logout handlers + cookie/login-bucket helpers) and `src/controlplane/http_api/app_routes.rs` (health/ready/UI fallback + dns-cache/stats handlers); keep `http_api.rs` focused on runtime wiring/state/router assembly.
- Runtime startup wiring now centralizes management/HTTP/metrics bind resolution in `src/runtime/bootstrap/startup.rs::resolve_bindings` (including Azure DPDK metrics bind override behavior).
- Cluster runtime/migration startup wiring is centralized in `src/runtime/bootstrap/startup.rs` via `start_cluster_runtime(...)` and `run_cluster_migration_if_requested(...)`; keep cluster bootstrap/migration orchestration there instead of growing `main.rs`.
- Wiretap/audit dataplane-to-controlplane bridge thread startup now lives in `src/runtime/startup/bridges.rs::spawn_event_bridges(...)`; keep bridge event enrichment/publish logic in that module.
- DNS/HTTP runtime thread startup now lives in `src/runtime/startup/controlplane_threads.rs` (`spawn_dns_runtime_thread`, `spawn_http_runtime_thread`, `HttpRuntimeThreadConfig`); keep thread/runtime bootstrap wiring there instead of `main.rs`.
- Integration-manager task startup now lives in `src/runtime/startup/integration_task.rs::spawn_integration_manager_task(...)`; keep integration ready-client fallback/task spawn wiring there instead of `main.rs`.
- Dataplane startup channel/task bootstrap now lives in `src/runtime/startup/dataplane_bootstrap.rs::bootstrap_dataplane_runtime(...)` (DPDK DHCP channels, DHCP task, IMDS fallback); keep that orchestration there instead of `main.rs`.
- Control-plane service orchestration now lives in `src/runtime/startup/controlplane_runtime.rs::start_controlplane_runtime(...)` (wiretap/audit bridge wiring, policy replication background task, Kubernetes resolver task, TLS-intercept readiness/generation setup, DNS startup gating, allowlist GC, and HTTP API thread startup). Keep this lifecycle wiring there instead of regrowing `main.rs`.
- Policy replication replay must run on leaders too (not just followers) so active cluster policy is restored into in-memory DNS/dataplane policy state after node restart without requiring a manual policy rewrite.
- Local non-cluster policy bootstrap and local control-plane storage path derivation are centralized in `src/runtime/bootstrap/policy_state.rs::init_local_controlplane_state(...)`; keep local policy replay/service-account+integration path derivation there rather than re-growing `main.rs`.
- Soft dataplane SNAT-auto interface warmup task is centralized in `src/runtime/bootstrap/dataplane_warmup.rs::maybe_spawn_soft_dataplane_autoconfig_task(...)`; keep retry/backoff wiring there instead of `main.rs`.
- HTTP API decomposition now also keeps large supporting blocks out of the router file: inline tests moved to `src/controlplane/http_api/tests.rs`, and cluster policy persistence/read helpers live in `src/controlplane/http_api/cluster_persistence.rs`.
- HTTP API policy activation helpers now live in `src/controlplane/http_api/policy_activation.rs` (`wait_for_policy_activation`, `enforcement_mode_for_policy_mode`); keep policy activation polling/mode mapping there rather than re-growing `http_api.rs`.
- TLS intercept CA API handlers (`GET/PUT/DELETE /api/v1/settings/tls-intercept-ca` and `POST /api/v1/settings/tls-intercept-ca/generate`) now live in `src/controlplane/http_api/tls_intercept.rs`; keep TLS intercept CA route/key parsing changes in that module rather than `http_api.rs`.
- Trafficd decomposition is in progress under `src/controlplane/trafficd/`; TLS intercept runtime handlers now live in `intercept_runtime.rs`, upstream TLS verification mode/connectors live in `upstream_tls.rs`, HTTP request/response parsing+match helpers live in `http_match.rs`, and service-lane/kernel steering setup now lives in `service_lane.rs`.
- TLS intercept HTTP matcher deny behavior now follows effective enforcement: deny/reset is only enforced when global policy enforcement mode is `enforce` and the matched intercept rule mode is `enforce`; in audit mode, deny matchers emit deny metrics without resetting traffic.
- TLS intercept certificate-domain logic now lives in `src/controlplane/trafficd/certs.rs` (leaf resolver/cache, host canonicalization, cert minting, and TLS acceptor builders); keep new cert-mint/resolver changes there rather than re-growing `trafficd.rs`.
- Trafficd inline tests were moved from `trafficd.rs` to `src/controlplane/trafficd/tests.rs`; keep new trafficd tests in that file to avoid re-growing runtime source.
- `trafficd` insecure verifier internals (`NoVerifier`) now live in `upstream_tls.rs`; `trafficd.rs` should avoid owning TLS connector internals as decomposition continues.
- DPDK adapter decomposition under `src/dataplane/dpdk_adapter/` now keeps `src/dataplane/dpdk_adapter.rs` as a thin shell; packet/frame parse/build helpers live in `frame_codec.rs`, service-lane TAP/env/mac helpers live in `service_lane.rs`, service-lane/demux runtime methods live in `service_lane_runtime.rs`, and frame-processing/DHCP/ARP/L2/runtime-loop methods live in `frame_runtime.rs`.
- DPDK adapter shared IO abstractions now live in `src/dataplane/dpdk_adapter/io_api.rs` (`FrameIo`, `UnwiredDpdkIo`); keep common IO trait/unwired shim there to avoid re-growing `dpdk_adapter.rs`.
- DPDK adapter DPDK backend runtime now uses `src/dataplane/dpdk_adapter/io.rs` as a thin shell; large domains are split into `src/dataplane/dpdk_adapter/io/{init_port,runtime,eal_port_select,tx_checksum}.rs`. Keep DPDK queue/runtime/EAL/offload changes in those domain files rather than re-growing `io.rs`.
- DPDK adapter unit tests were moved from inline `#[cfg(test)]` in `dpdk_adapter.rs` to `src/dataplane/dpdk_adapter/tests.rs`; test-case domains are further split under `src/dataplane/dpdk_adapter/tests/{frame_arp_dhcp_cases,intercept_overlay_cases,service_lane_cases}.rs` and included from the root test module.
- Dataplane engine decomposition now splits `src/dataplane/engine.rs` into domain modules under `src/dataplane/engine/`: `no_snat.rs` (no-SNAT + DNS-target paths), `common.rs` (shared TTL/audit/wiretap/TLS-policy helpers), `packet_path.rs` (main packet policy/NAT runtime path), and ICMP handlers under `icmp/` (`outbound.rs`, `inbound.rs`) with `icmp.rs` kept as a thin include shell.
- Dataplane policy decomposition now keeps `src/dataplane/policy.rs` as a thin re-export shell; core policy/domain types live in `src/dataplane/policy/model.rs`, dynamic CIDR/IP-set logic lives in `src/dataplane/policy/ip_sets.rs`, evaluation flow lives in `src/dataplane/policy/evaluation.rs`, TLS metadata evaluation in `src/dataplane/policy/tls_eval.rs`, and policy unit tests in `src/dataplane/policy/tests.rs`.
- TLS intercept upstream TLS verification in `trafficd` defaults to strict system-root certificate validation; set `NEUWERK_TLS_INTERCEPT_UPSTREAM_VERIFY=insecure` only for controlled compatibility/testing scenarios.
- Cluster store CF handle lookup no longer panics on missing column families; it now returns `StorageError` and propagates through raft/store operations.
- Raft network client factory (`RaftGrpcNetworkFactory::new_client`) no longer uses panic paths for invalid peer endpoint/TLS config; it now logs and degrades to transport failure via an unreachable fallback channel.
- Service account tokens are managed via HTTP API: `POST /v1/service-accounts`, `GET /v1/service-accounts`, `DELETE /v1/service-accounts/{id}`, `POST /v1/service-accounts/{id}/tokens`, `GET /v1/service-accounts/{id}/tokens`, `DELETE /v1/service-accounts/{id}/tokens/{token_id}`.
- Token creation defaults to 90d TTL or `eternal: true`; token strings are returned only on create.
- Prometheus metrics are served over HTTP on `--metrics-bind` (default management IP `:8080`) at `/metrics`.
- A ready-to-import Grafana dashboard for the exposed firewall metrics is shipped at `docs/grafana/firewall-operations-dashboard.json`.
- HTTP API startup now rejects public/non-private metrics binds by default; set `NEUWERK_ALLOW_PUBLIC_METRICS_BIND=1` only when intentionally exposing metrics beyond private/loopback/link-local ranges.
- Integration `api_server_url` must be a valid absolute URL and `https://` for non-loopback hosts; `http://` is accepted only for loopback test endpoints (`localhost`, `127.0.0.1`, `::1`).
- `NEUWERK_DPDK_STATE_SHARDS=<n>` overrides the number of dataplane state shards (defaults to worker count); sharding reduces lock contention in multi-worker DPDK mode.
- DPDK worker pinning now derives a core list from Linux CPU topology (prefers one logical CPU per physical core first), exports it via `NEUWERK_DPDK_CORE_IDS`, and uses that list for both EAL `-l` and per-worker `pthread_setaffinity_np` pinning.
- When DPDK has only one effective RX queue, runtime can still use multiple workers via shared-RX software flow demux (single DPDK RX queue with flow-affine worker dispatch); HTTPS/TLS flows on TCP/443 are pinned to worker `0` so service-lane intercept steering remains deterministic.
- In multi-worker DPDK mode, only worker `0` should drain service-lane egress frames; draining from every worker can create cross-shard lock contention and inflate `dp_state_lock_*` metrics.
- API auth CLI: `firewall auth key rotate|list|retire <kid>` and `firewall auth token mint --sub <id> [--ttl <dur>] [--kid <kid>]` require `--cluster-addr <ip:port>` and mTLS material in `--cluster-tls-dir` (default `/var/lib/neuwerk/cluster/tls`).
- DNS hostname access control is configured via policy YAML rule matches using `dns_hostname` (regex); unmatched DNS queries return NXDOMAIN.
- Policy YAML rules can match ICMP with `icmp_types` and `icmp_codes` lists. For `proto: icmp` rules with no ICMP filters, defaults apply: types `[0, 3, 11]` and codes `[0, 4]` (echo-reply, dest-unreachable, time-exceeded, frag-needed).
- Cluster joiners must use `--join <seed-endpoint>` and a per-node PSK for unattended bootstrap.
- The initial seed generates the cluster CA; CA key material is stored in the embedded replicated store so any control-plane leader can sign node certs.
- Each node generates a UUID once and stores it at `/var/lib/neuwerk/node_id`.
- Any node can act as the initial seed if started without `--join`; control-plane RPC is strictly internal (external API is separate transport).
- Future deployments will use cloud APIs (ASG/instance tags + age) for peer discovery; local e2e tests use static IPs.
- The bootstrap PSK lives at `/var/lib/neuwerk/bootstrap-token` and should support rotation.
- The bootstrap token file uses a JSON format; join uses a single seed endpoint. Token entries support `valid_from`/`valid_until` windows for unattended rotation.
- Bootstrap token file permissions are enforced on Unix by `TokenStore::load(...)`; files must not be group/world accessible (use `0600` in fixtures/deployments).
- Bootstrap token rotation model: publish overlapping old/new `kid` windows, joiners pick the newest active window, and seeds accept only active windows for the presented `kid`.
- Cluster RPC binds with `--cluster-bind <ip:port>` and advertises via `--cluster-advertise <ip:port>` (default: bind).
- Join RPC binds with `--cluster-join-bind <ip:port>` (default: `cluster-bind` + 1).
- Cluster storage defaults to `/var/lib/neuwerk/cluster` and can be overridden via `--cluster-data-dir`.
- Node identity path defaults to `/var/lib/neuwerk/node_id` and can be overridden via `--node-id-path`.
- Bootstrap token path defaults to `/var/lib/neuwerk/bootstrap-token` and can be overridden via `--bootstrap-token-path`.
- Raft control-plane RPC uses mTLS; join RPC uses PSK-authenticated bootstrap over the join bind address and encrypts returned TLS material at the message layer (no out-of-band CA distribution needed).
- Join bootstrap responses include `response_hmac` (HMAC-SHA256 over request identity + encrypted payload) keyed by the bootstrap PSK; joiners must verify and decrypt before writing `tls/node.crt` and `tls/ca.crt`.
- The UI is embedded from `ui/dist` at build time. Rebuild the UI bundle before building the Rust binary when UI assets change.

## Integration Tests
- `make test.integration` runs the Rust e2e harness which builds Linux netns/veth topology via netlink; it must be run as root.
- `make test.integration` now also runs `e2e_kind_harness`, which creates a temporary `kind` cluster and validates Kubernetes List/Watch-driven dynamic source resolution against real cluster objects; this requires working Docker, `kind`, and `kubectl` on the test host.
- `src/bin/e2e_kind_harness.rs` now keeps scenario orchestration and policy builders; Kubernetes API discovery/kubectl/pod label/wait/evaluation helpers live in `src/bin/e2e_kind_harness/k8s_helpers.rs`.
- Production-readiness gate targets are `make test.readiness.unit`, `make test.readiness.integration`, and aggregate `make test.readiness`; they run focused durability/failover/drain/SLO readiness cases without invoking root-required netns e2e.
- `test.readiness.unit` now includes a cluster-RPC resilience regression (`controlplane::cluster::rpc::tests::raft_network_factory_handles_invalid_addr_without_panic`) to guard against panic regressions in raft peer client construction.
- Cluster RPC test-only assertions now live in `src/controlplane/cluster/rpc/tests.rs`; keep `src/controlplane/cluster/rpc.rs` as the thin shell and place runtime transport/client changes in `rpc/management.rs` or `rpc/raft_transport.rs`.
- Cluster RPC is now decomposed under `src/controlplane/cluster/rpc/`: `management.rs` owns join/policy/integration/auth/wiretap clients+servers, `raft_transport.rs` owns raft network/factory/server transport wiring, and `rpc.rs` is a thin shell for `RaftTlsConfig`, `proto`, and stable re-exports.
- `e2e_kind_harness` now covers pod/node selector enforcement, stale-IP grace expiry/recovery via integration updates, namespace scoping, selector union+dedupe, priority interactions (dynamic deny over static allow), and pod/node churn behavior.
- Cluster e2e includes `cluster_kubernetes_resolver_leader_failover`, which runs active Kubernetes resolvers on a 3-node control-plane cluster, forces leader failover, and verifies dynamic source updates still drive policy allow/deny decisions afterward.
- `tests/http_api.rs` is now a thin integration-test root; HTTP API integration scenarios live under `tests/http_api/` split by domain (`lifecycle_cases`, `readiness_policy_cases`, `cluster_audit_cases`, `wiretap_auth_cases`) and are wired with explicit `#[path = "http_api/..."]` module attributes.
- `tests/cluster_join.rs` is now a thin integration-test root; cluster lifecycle/failover/replay scenarios live under `tests/cluster_join/` split by domain (`membership_cases`, `lifecycle_cases`, `replay_quorum_cases`, `failover_cases`) and are wired with explicit `#[path = "cluster_join/..."]` module attributes.
- `tests/packet_unit.rs` now keeps shared packet/policy/checksum helper builders and wires the main packet behavior assertions from `tests/packet_unit/cases.rs` via explicit `#[path = "packet_unit/cases.rs"]`.
- Initial fuzz harness now lives under `fuzz/` with `cargo-fuzz` targets `packet_parse`, `overlay_decap`, and `tls_reassembly`; build-check via `cargo check --manifest-path fuzz/Cargo.toml`.
- Fuzz workflow entry points: `make fuzz.check` (compile harness), `make fuzz.smoke` (bounded smoke runs via `scripts/fuzz-smoke.sh`), `make fuzz.nightly` (time-bounded sanitizer runs via `scripts/fuzz-nightly.sh`), and `make test.readiness.fuzz`; both smoke/nightly lanes invoke `cargo +nightly fuzz ...` and require nightly Rust for sanitizer-enabled execution.
- `make fuzz.smoke` now enforces `NEUWERK_FUZZ_REQUIRED=1` in the Makefile target, so missing `cargo-fuzz`/nightly fails readiness gates instead of silently passing.
- Fuzz seed corpora under `fuzz/corpus/*` now include protocol-shaped seeds (IPv4 TCP/UDP/ARP, overlay mode/port variants, TLS fragmented/two-way record streams) in addition to minimal bootstrap seeds.
- GitHub Actions fuzz lanes now exist under `.github/workflows/`: `fuzz-smoke.yml` (PR/push on `main`, nightly toolchain) and `fuzz-nightly.yml` (cron + manual dispatch).
- GitHub Actions full-suite CI lane now exists under `.github/workflows/ci-full-suite.yml` and runs on PR + push `main` (`cargo fmt --all --check`, `npm --prefix ui test`, `npm --prefix ui run build`, `cargo test -- --test-threads=1`).
- Fuzz uncovered a malformed-IPv4 panic path in `Packet::recalc_checksums`; checksum recomputation now rejects `total_len < ihl` via checked arithmetic in `src/dataplane/packet/impl_packet/rewrites.rs` and is guarded by `dataplane::packet::tests::recalc_checksums_rejects_malformed_ipv4_total_len_smaller_than_ihl`.
- `tests/http_api/wiretap_auth_cases.rs` now asserts query-token access to `/api/v1/wiretap/stream` is rejected (`http_api_wiretap_stream_local_query_token_is_rejected`), matching hardened cookie/bearer-only auth behavior.
- Cluster HTTP API proxying in tests assumes a uniform HTTPS API port across cluster nodes; `http_api_cluster_proxy_lifecycle` keeps a shared per-test HTTP port on both nodes while using dynamic ports for other listeners.
- Running integration tests as root can leave `target/` root-owned; if builds fail with permission errors, `sudo chown -R $USER target` or remove `target/`.
- Every feature that changes control-plane or data-plane behavior must be covered by an integration test.
- The e2e harness now includes control-plane cluster checks (mTLS enforcement and leader failover join) inside the firewall netns.
- Favor e2e coverage over unit tests to preserve freedom to replace internal implementations; unit tests are still valuable for correctness.
- The cluster e2e suite is now decomposed under `src/e2e/cluster_tests/`; keep `src/e2e/cluster_tests.rs` as a thin orchestrator and add new cases in the domain files (`core_cases`, `failover_cases`, `kubernetes_failover_case`, `migration_cases`, `api_cases`).
- The HTTP API cluster/audit integration suite is now decomposed under `tests/http_api/cluster_audit_cases/`; keep `tests/http_api/cluster_audit_cases.rs` as a thin `#[path]` shim and add new tests in `proxy_wiretap_cases.rs` or `audit_cases.rs`.
- The e2e harness now runs overlay VXLAN and GENEVE suites after baseline tests using `overlay_vxlan_*` and `overlay_geneve_*` fields in `TopologyConfig`.
- e2e case registration is split into `src/e2e/tests/case_catalog.rs`, with domain scenarios now extracted into `src/e2e/tests/api_cases.rs`, `src/e2e/tests/api_auth_cases.rs`, `src/e2e/tests/dpdk_cases.rs`, `src/e2e/tests/overlay.rs`, `src/e2e/tests/traffic_cases.rs`, `src/e2e/tests/tls_cases.rs`, and `src/e2e/tests/dns_flow_cases.rs`; `src/e2e/tests/helpers.rs` is a thin wiring module and shared utilities are split across helper-domain modules (`helpers_overlay.rs`, `helpers_policy.rs`, `helpers_dns_net.rs`, `helpers_raw_socket.rs`, `helpers_l2_dhcp.rs`).
- API-domain e2e scenarios are further split under `src/e2e/tests/api_cases/`: `audit_cases.rs` contains audit-focused scenarios and `policy_metrics_cases.rs` contains policy/bootstrap/cache/metrics API scenarios; `api_cases.rs` is now a thin orchestrator.
- Cluster e2e reusable helpers in `src/e2e/cluster_tests.rs` are now split into `src/e2e/cluster_tests/helpers.rs`; migration scenarios are in `src/e2e/cluster_tests/migration_cases.rs`; cluster HTTP/audit scenarios are in `src/e2e/cluster_tests/api_cases.rs`.
- Cloud control-plane runtime/test split now keeps `src/controlplane/cloud/mod.rs` runtime-focused and places its unit tests in `src/controlplane/cloud/tests.rs`; Azure-provider tests live in `src/controlplane/cloud/providers/azure/tests.rs`.
- Cloud control-plane unit tests are now further decomposed under `src/controlplane/cloud/tests/cases/`; keep `src/controlplane/cloud/tests/cases.rs` as a thin include shell and add new tests in `assignment_cases.rs`, `reconcile_cases.rs`, or `termination_cases.rs`.
- `src/controlplane/cloud/providers/azure.rs` is now a thin shell (constants + constructor + includes). Azure API payload models/scheduled-event structs live in `src/controlplane/cloud/providers/azure/models.rs`, shared provider helpers live in `src/controlplane/cloud/providers/azure/helpers.rs`, and CloudProvider method-domain logic is split under `src/controlplane/cloud/providers/azure/provider_impl/{identity,discovery,routes,lifecycle}.rs` with a delegating trait impl in `provider_impl.rs`.
- `src/controlplane/metrics.rs` now keeps snapshot/render shell logic while constructor wiring is in `src/controlplane/metrics/construct.rs` and metric mutation methods are in `src/controlplane/metrics/methods.rs`.
- `src/dataplane/packet.rs` is now a thin shell (types/constants/module wiring); packet method implementations are decomposed under `src/dataplane/packet/impl_packet/{core,icmp,ip_ports,checksum_updates,rewrites}.rs` with `impl_packet.rs` as an include shell, checksum helpers stay in `src/dataplane/packet/checksum_helpers.rs`, and module unit tests stay in `src/dataplane/packet/tests.rs`.
- `src/controlplane/dns_proxy.rs` keeps runtime DNS proxy logic; its unit tests live in `src/controlplane/dns_proxy/tests.rs`.
- E2E includes `api_audit_passthrough_overrides_deny` to assert audit mode is pass-through (deny rules do not block DNS traffic while mode is `audit`).
- TLS intercept fail-closed e2e can manifest as TCP reset/broken pipe or immediate connect refusal (`ECONNREFUSED`); treat both as valid fail-closed outcomes.
- E2E includes `tls_intercept_h2_concurrency_smoke` to stress HTTP/2 intercept allow-path behavior under concurrent load.
- E2E includes `tls_intercept_ca_rotation_reloads_runtime` to verify CA rotation updates served intercept leaf certificates while preserving allow/deny policy behavior.
- The root-required e2e harness launches the firewall with `--data-plane-mode tun`; DPDK service-lane regressions are covered by in-process DPDK e2e cases like `dpdk_tls_intercept_service_lane_round_trip`.

## Completed (Test Gaps)
- TTL decrement test asserts forwarded UDP TTL is reduced by exactly 1.
- ICMP policy coverage includes deny cases and type/code filtering.
- Fragment-forwarding negative test ensures fragments are not forwarded.
- NAT determinism coverage adds port allocation stability under load.
- Overlay negative tests cover wrong VNI/UDP port/MTU drop.
- Audit findings query coverage (`/api/v1/audit/findings`) currently exists in `tests/http_api.rs`; there is no netns e2e case yet that generates live dataplane audit events and verifies end-to-end retrieval.

## Cloud Tests
- AWS e2e bench lives under `cloud-tests/aws` (Terraform + scripts) and is single-AZ GWLB/GENEVE with DPDK dataplane.
- For transparent inline inspection on AWS, NLB cannot replace GWLB directly: NLB is listener/port based and rewrites destination to the target, so the firewall does not see the original upstream destination tuple. To isolate GWLB/GENEVE overhead, prefer a separate no-encap architecture that routes consumer/upstream subnets to the firewall dataplane ENI (`source_dest_check=false`) instead of tunneling.
- In no-SNAT + no-encap AWS mode, inbound reverse traffic must still run the no-SNAT reverse-flow path. A prior guard tied reverse matching to overlay mode and caused SYN-ACK blackholing in `eni_no_encap`; dataplane now matches reverse flows for all no-SNAT traffic and has a regression test `no_snat_plain_inbound_reverse_flow_round_trip`.
- AWS GWLB e2e should keep `NEUWERK_GWLB_TUNNEL_SRC_PORT=0` and `NEUWERK_GWLB_SWAP_TUNNELS=0`; forcing either to `1` has shown severe throughput collapse/instability in the single-tunnel AWS topology.
- For the current AWS bench topology, both consumer->upstream and upstream->consumer subnet routes are steered through the consumer-side GWLBE to keep flow symmetry.
- AWS upstream SG must allow ICMP from `consumer_subnet_cidr` for `icmp_echo_allow` smoke validation.
- AWS ENA in this bench can report unreliable queue caps via `rte_eth_dev_info` (for example `max_rx_queues=9234`, `max_tx_queues=0`) while `testpmd` still supports `rxq/txq=4`; queue setup should be probe-driven (attempt requested queues, then fall back) rather than hard-clamped to those fields.
- On AWS ENA with DPDK `.so.26`, `rte_eth_dev_configure` may fail with explicit `rss_hf` (`ret=-22`); retrying with `rss_hf=0` succeeds and ENA uses default RSS hash fields (`0xc30`).
- AWS ENA on Ubuntu 24.04 (`dpdk` apt packages) can fail TX queue setup with `Tx conf reserved fields not zero` if userspace passes `dev_info.default_txconf`; prefer `rte_eth_tx_queue_setup(..., NULL)` (PMD defaults) in this bench to avoid txconf ABI/layout mismatches.
- On AWS `eni_no_encap` (`c6in.large`, Ubuntu 24.04, DPDK `.so.24`), ENA reports stable queue caps with the current runtime-safe accessors (`max_rx_queues=2`, `max_tx_queues=2`) and queue-per-worker starts cleanly.
- On that same bench, enabling `NEUWERK_DPDK_TX_CSUM_OFFLOAD=1` is now startup-stable but regresses throughput (`iperf3 -P 8 -t 60`: ~`9.64` Gbps -> ~`7.38` Gbps) and sharply increases lock contention (`dp_state_lock_contended_total`: ~`62k` -> ~`1.28M`); keep TX checksum offload disabled by default (`NEUWERK_DPDK_TX_CSUM_OFFLOAD=0` or unset).
- AWS GWLB/GENEVE throughput is sensitive to `--encap-mtu`; `--encap-mtu=1800` plus `NEUWERK_DPDK_PORT_MTU=1800` remains the best-known baseline in this bench.
- In `cloud-tests/aws`, `terraform apply` updates S3 objects but does not force running firewall VMs to re-download artifacts after bootstrap (`/var/lib/neuwerk/bootstrap.complete`); when iterating binaries on a live stack, copy `/usr/local/bin/firewall` to the instance and restart `firewall.service` (or recreate the instance).
- AWS Ubuntu 24.04 images provide DPDK ABI `.so.24`; preloading a runtime bundle built for `.so.26` can fail startup with `EAL: Cannot init trace` (duplicate trace entries). Keep firewall/runtime DPDK ABI aligned, or skip runtime preload envs when running `.so.24` builds.
- AWS Ubuntu 24.04 `dpdk` package updates (eg `23.11.4`) can diverge from vendored `23.11.2` headers while keeping `.so.24`; `rte_eth_dev_info` layout differences (eg added MTU/buffer fields) can make queue-cap fields appear bogus (`max_rx_queues=9234`, `max_tx_queues=0`) in binaries compiled against older headers. Build against target-host DPDK headers/libs (or ship matching runtime) to avoid struct-layout drift.
- On the AWS GWLB bench (`c6in.xlarge`) with queue-per-worker mode active (`4` workers/queues), current measured throughput is ~2.5-2.6 Gbps (`iperf3`, `P=4..16`), versus ~1.0-1.6 Gbps in prior single-queue runs.
- In the same AWS setup, increasing `NEUWERK_DPDK_STATE_SHARDS` above worker-count reduces lock contention and improves throughput; observed with `P=16`: shards `4` ~2.2 Gbps, shards `8-16` ~2.62-2.65 Gbps, shards `32` ~2.66 Gbps.
- In the same AWS setup, toggling `NEUWERK_DPDK_OVERLAY_DEBUG` (`0` vs `1`) showed no material throughput change in quick A/B runs; the remaining bottleneck is elsewhere in packet processing.
- DPDK workers now run service-lane/DHCP housekeeping on an interval (`NEUWERK_DPDK_HOUSEKEEPING_INTERVAL_PACKETS`, default `64`; `NEUWERK_DPDK_HOUSEKEEPING_INTERVAL_US`, default `250`) instead of every packet on worker `0`; this improved AWS GWLB throughput to ~2.7-2.9 Gbps (`P=4`) on `c6in.xlarge`.
- AWS shard-guard lock pinning (`NEUWERK_DPDK_PIN_STATE_SHARD_GUARD`) is currently safest with default `off`; A/B on `c6in.xlarge` showed higher lock wait time and retransmits when enabled, with tail outliers at higher parallel stream counts.
- On that same bench, increasing housekeeping interval too aggressively (`256` packets / `1000` us) reduced `P=4` throughput (~2.0 Gbps), so keep defaults unless profiling indicates otherwise.
- AWS smoke defaults intentionally skip `tls_intercept_http_path_enforcement`; override via `RUNNER_ARGS`/`AWS_RUNNER_TESTS` when validating TLS intercept specifically.
- Azure e2e test bench lives under `cloud-tests/azure` (Terraform + scripts); SSH keys are generated under `cloud-tests/.secrets` and are gitignored.
- Azure DPDK dataplane LB probe reliability can degrade if the process binds metrics on `0.0.0.0:8080` (kernel listener on dataplane IP races dataplane probe replies). Prefer `--metrics-bind <management-ip>:8080` in Azure DPDK deployments.
- Azure GWLB service chaining requires a Standard **public** LB frontend (or VM public IP). Internal LBs cannot be chained, and a NIC associated to GWLB must keep a Standard public IP attached. For private-only dataplane tests, use UDR + internal LB instead of GWLB.
- For the internal LB + UDR Azure e2e topology, set `firewall_snat_mode=none` so the upstream sees the original consumer IP; SNAT can break multi-connection protocols like `iperf3` when flows land on different firewall nodes.
- Azure internal LB + UDR e2e defaults `firewall_snat_mode=none` so return traffic stays symmetric; SNAT can break consumer->upstream VIP flows.
- `cloud-tests/azure/scripts/run-tests.sh` throughput smoke test uses a 4 MiB payload and `socat -T 5` to avoid hanging on close; increase size/timeouts if you want larger throughput checks.
- DPDK dataplane exports `dpdk_rx_*`/`dpdk_tx_*` counters for throughput debugging in `/metrics`.
- DPDK multi-worker sharding exports `dp_state_lock_wait_seconds`/`dp_state_lock_contended_total` plus per-queue counters to validate RSS distribution.
- On some Azure MANA/netvsc setups, DPDK reports `flow_type_rss_offloads=0` (no usable RSS); runtime now auto-falls back to a single queue/worker even when `NEUWERK_DPDK_WORKERS>1` to avoid multi-worker throughput regressions.
- Azure dataplane reliability on internal LB/UDR can degrade in multi-worker mode (intermittent return-path loss for consumer<->upstream VIP flows). Runtime now enables an Azure reliability guard that forces a single DPDK worker by default; override only for controlled validation via `NEUWERK_DPDK_ALLOW_AZURE_MULTIWORKER=1`.
- In local (non-cluster) Azure e2e mode, API auth keysets are node-local (`/var/lib/neuwerk/http-tls/api-auth.json`), so a single token via the mgmt LB may fail on some nodes; cloud policy-smoke orchestration must mint per-node tokens and push/test policy against each firewall mgmt IP.
- Azure policy-smoke UDP allow tests require an upstream ILB rule for UDP/5201; without it, allow-case validation fails even when dataplane policy is correct.
- Azure load balancers do not forward ICMP, so policy-smoke ICMP tests must target `upstream_private_ip` (routed via UDR through the firewall), not the upstream ILB VIP.
- The common smoke test `tls_intercept_http_path_enforcement` requires a firewall image that serves `GET/PUT/DELETE /api/v1/settings/tls-intercept-ca` and preserves `tls.mode=intercept` + HTTP matchers; older images typically return UI HTML on that settings path and silently drop intercept-specific TLS fields during policy create.
- Azure lifecycle rollout validation is scripted in `cloud-tests/azure/scripts/lifecycle-rollout.sh`: it runs sustained mixed short-lived traffic from a consumer VM (`dns_udp`, `dns_tcp`, `http`, `https`, `delayed_http`), performs a Flexible-compatible VMSS surge+replace rollout (capacity +1 then delete old instance per step), enforces zero traffic errors, and captures per-instance integration/DPDK metrics snapshots under `cloud-tests/azure/artifacts/`.
- Azure termination-drain validation is scripted in `cloud-tests/azure/scripts/lifecycle-termination-drain.sh`: it supports `TRIGGER_ACTION=terminate|reboot`, samples target metrics continuously, and asserts streamed max increases for `integration_termination_events_total` and `integration_termination_drain_start_seconds_count` so reboot counter resets do not hide detections.
- Azure HTTP webhook perf harness is scripted under `cloud-tests/azure/scripts/http-perf-*.sh` with scenario policies in `cloud-tests/azure/policies/http-perf/`, k6 load profile in `cloud-tests/azure/http-perf/k6/webhook.js`, and Make targets `http-perf.setup|http-perf.quick|http-perf.run`; runs emit JSON artifacts under `cloud-tests/azure/artifacts/http-perf-*` and `http-perf-matrix-*`.
- Azure integration identity must use VMSS `instance_id` consistently (not IMDS `vm_id` UUID), otherwise termination keys and drain control will not line up with discovered instances.
- Azure ARM throttling can spike when reconciliation is too aggressive across all nodes; keep `--integration-reconcile-interval-secs` in a moderate range (for e2e currently 15s).
- GCP VPC static routes cannot override subnet-local routes (even `/32` inside another subnet CIDR), so Azure-style UDR steering inside one VPC must use guest-policy routes on consumer/upstream VMs that point to subnet-local internal passthrough ILB VIPs.
- GCP firewall instance-template NICs now set explicit gVNIC `queue_count`: management NIC uses `firewall_mgmt_queue_count` (default `1`), dataplane NIC uses `firewall_total_nic_queue_count - firewall_mgmt_queue_count` (default `7` on n2-standard-8 with total `8`).
- GCP DPDK firewall templates should keep the dataplane NIC as the primary interface and management NIC as secondary; otherwise ILB backend traffic can land on the management interface and bypass the DPDK dataplane.
- In the GCP firewall bootstrap, pin the default route via `mgmt0` before/while binding the dataplane NIC to `vfio-pci`; otherwise management-plane reachability (SSH/health/API) can drop after dataplane bind.
- GCP policy-based routes must use subnet-local dataplane ILB next hops per source subnet (`consumer -> consumer dataplane ILB`, `upstream -> upstream dataplane ILB`); pointing both directions to the dataplane-subnet ILB can blackhole traffic.
- On GCP net_gve, `rte_eth_dev_info` queue caps can be unreliable (`max_rx_queues` very large, `max_tx_queues=0`), so queue setup should be probe-driven instead of hard-clamped from those fields.
- On current GCP Ubuntu 24.04 images with net_gve and DPDK, n2-standard-8 accepted up to 2 RX/TX queues in this bench, while n2-standard-4 rejected `nb_rx_queues>1` (`rte_eth_dev_configure` returned `-22`).
- For GCP internal passthrough ILB + DPDK dataplane tests, backend health checks must use TCP on dataplane `:8080` (DPDK health-probe path). HTTPS `/ready` on `:8443` is management-plane only and is not reachable via the dataplane NIC once it is bound to VFIO.
- In `cloud-tests/gcp`, running plain `terraform apply` without explicit machine-type vars will revert instances to defaults from `variables.tf`, which can invalidate n2-standard-8 perf baselines.
- Replacing all four GCP bench instances to `n2-standard-8` can hit regional CPU quota (`CPUS_ALL_REGIONS=32`) during MIG surge replacement; avoid by planning capacity for temporary surge or using a non-surge replacement strategy.
